var React = require('react');

var ToDoApp = React.createClass({
    render: function () {
        return (
            <div>
                ToDoApp is rendered.
            </div>
        )
    }
});

module.exports = ToDoApp;